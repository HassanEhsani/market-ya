import SwiftUI
import Foundation

struct ContentView: View {
    var body: some View {
        Text("Hello Firuz 👋")
            .font(.title)
            .foregroundColor(.green)
            .padding()
    }
}

